<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="curtin.edu.assignment01">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.ResultActivity"></activity>
        <activity android:name=".activity.MarkingActivity" />
        <activity android:name=".activity.PracDetailActivity" />
        <activity android:name=".activity.AddPracActivity" />
        <activity android:name=".activity.StudentDetailActivity" />
        <activity android:name=".activity.AddStudentActivity" />
        <activity android:name=".activity.InstructorDetailActivity" />
        <activity android:name=".activity.AddInstructorActivity" />
        <activity android:name=".activity.NavigationActivity" />
        <activity android:name=".activity.SignUpActivity" />
        <activity android:name=".activity.LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>